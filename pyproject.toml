[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "room-ui"
version = "0.1.0"
description = "Voice Assistant — PySide6 desktop app wrapping roomkit"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.7",
    "qasync>=0.27",
    "roomkit[realtime-gemini,realtime-openai,local-audio]",
    "pynput>=1.7",
]

[project.optional-dependencies]
dev = ["pyinstaller>=6.0"]

[project.scripts]
room-ui = "room_ui.app:main"
